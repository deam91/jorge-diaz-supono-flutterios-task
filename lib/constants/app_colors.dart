import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const black = Colors.black;
  static const white = Colors.white;

  // Gray scale
  static const gray = Color(0xff959595);
  static const darkGray = Color(0xFF8E8E93);

  // Background colors
  static const settingsGroupBg = Color(0xff4b4053);
  static const dividerColor = Color(0xFF3E3744);

  // Transparent backgrounds
  static const blackTransparent = Color(0x4D000000); // 30% opacity

  // Button colors
  static const buttonBorderColor = Color(0xff959595);

  // Text colors
  static const subtitleGray = Color(0xffA6A6A6);
}
